[phases.setup]
nixPkgs = ['php81', 'php81Packages.composer', 'mysql']

[phases.install]
cmds = ['composer install --no-dev --optimize-autoloader']

[phases.build]
cmds = ['php artisan config:clear', 'php artisan storage:link']

[phases.start]
cmd = 'php artisan serve --host=0.0.0.0 --port=$PORT'
